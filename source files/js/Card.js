//This is the simplest class.
// It represents a card, which has three attributes:rank(String, it can be like 2, 3 or ace as wel), suit(string), realValue (integer).
class Card
{
	constructor(rank,realValue, suit)
	{
	this.rank = rank;
	this.suit = suit;
	this.realValue = realValue;
	}	
}